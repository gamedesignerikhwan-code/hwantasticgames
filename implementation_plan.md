# 80% 조기종료 시스템 - 목표 승률 정밀 매칭 최적화

## 문제 분석

현재 [calibrated_80percent_early_exit.py](file:///g:/내 드라이브/Board Game Ideas/291. Co-op Climbing/calibrated_80percent_early_exit.py)의 데이터를 보면 심각한 문제가 있습니다:

### LV1~2 변별력 부재 & 목표 승률 불일치

| LV | 목표승률 | 2인 | 3인 | 4인 | 5인 |
|----|---------|-----|-----|-----|-----|
| 01 | **85%** | 100% ❌ | 100% ❌ | 41.9% ❌ | 13.3% ❌ |
| 02 | **82%** | 100% ❌ | 100% ❌ | 41.8% ❌ | 10.0% ❌ |
| 03 | 78% | 100% ❌ | 79.7% ✅ | 34.3% ❌ | 11.6% ❌ |

> [!CAUTION]
> **전 레벨에 걸쳐 목표 승률과 실제 승률의 괴리가 매우 큽니다.** 2인/3인은 대부분 100%로 포화되어 너무 쉽고, 4인은 30~45%대로 목표의 절반 수준, 5인은 10~13%로 처참합니다.

### 근본 원인

1. **옵티마이저가 없었음**: [optimize_80percent_early_exit.py](file:///g:/내 드라이브/Board Game Ideas/291. Co-op Climbing/optimize_80percent_early_exit.py)는 고정된 카드 배분/턴을 시뮬레이션만 하고, **목표 승률에 맞추기 위한 탐색(search)을 하지 않음**
2. **인원별 독립 탐색 부재**: 2/3/5인의 카드 배분과 턴 수가 4인 기준에서 기계적으로 변환되었을 뿐, 각 인원수별로 목표 승률에 맞는 최적 조합을 탐색하지 않음
3. **LV1-2 동일한 파라미터**: 두 레벨이 완전히 같은 배분을 사용하여 변별력이 0

## 해결 방향

> [!IMPORTANT]
> 핵심은 **각 인원수(2/3/4/5인)별로 독립적으로** 턴 수와 카드 배분을 탐색하여, 시뮬레이션 승률이 목표 승률의 ±5%p 이내에 들어오도록 최적화하는 것입니다.

### 조정 가능한 변수 (난이도 조절 레버)

| 변수 | 방향 | 효과 |
|------|------|------|
| **턴 수** 증가 | 쉬움 ↑ | 조기종료 달성 기회 증가 |
| **턴 수** 감소 | 어려움 ↑ | 시간 부족으로 실패 증가 |
| **카드 배분 균등화** (예: [6,6,5,5]) | 쉬움 ↑ | 오차 범위 진입 용이 |
| **카드 배분 편중** (예: [8,6,5,3]) | 어려움 ↑ | 정밀한 트릭 배분 필요 |
| **margin** 증가 (2→3) | 쉬움 ↑ | 허용 오차 증가 |

## Proposed Changes

### 새로운 정밀 옵티마이저

#### [NEW] [reoptimize_80percent.py](file:///g:/내 드라이브/Board Game Ideas/291. Co-op Climbing/reoptimize_80percent.py)

**40개 레벨 × 4개 인원수를 각각 독립 최적화하는 스크립트**:

1. **시뮬레이션 엔진**: 기존 `sim_early_exit()`를 그대로 사용 (검증된 로직)
2. **인원별 카드 배분 후보 풀 대폭 확장**:
   - **2인 (총 16장 목표)**: `[8,8]`, `[9,7]`, `[10,6]`, `[11,5]` + 고스트 `[16,0]`
   - **3인 (총 21장 목표)**: `[7,7,7]`, `[8,7,6]`, `[9,7,5]`, `[9,6,6]`, `[10,6,5]` + 고스트 `[12,9,0]`, `[11,10,0]`
   - **4인 (총 22장 목표)**: `[6,6,5,5]`, `[6,6,6,4]`, `[7,6,5,4]`, `[7,5,5,5]`, `[8,6,5,3]`, `[8,5,5,4]` + 고스트 `[8,7,7,0]`, `[9,7,6,0]`, `[7,8,7,0]`
   - **5인 (총 24장 목표)**: `[5,5,5,5,4]`, `[5,5,5,4,5]`, `[6,5,5,4,4]`, `[6,5,4,5,4]`, `[7,5,5,4,3]`, `[6,6,4,4,4]` + 고스트 `[8,8,8,0,0]`, `[10,8,6,0,0]`
3. **턴 수 탐색 범위**: 인원수별 적정 범위에서 넓게 탐색
   - 2인: 6~10턴
   - 3인: 6~10턴
   - 4인: 7~11턴
   - 5인: 5~9턴
4. **목표 매칭 알고리즘**: 각 (카드배분, 턴수) 조합을 시뮬레이션(5000회)하여 목표 승률과의 차이가 최소인 조합을 선택
5. **변별력 보장**: LV1과 LV2의 결과가 동일하지 않도록, 적어도 하나의 파라미터(턴 수 또는 배분)가 다르게 강제

---

#### [MODIFY] [calibrated_80percent_early_exit.py](file:///g:/내 드라이브/Board Game Ideas/291. Co-op Climbing/calibrated_80percent_early_exit.py)

- 새 옵티마이저 실행 결과로 `EARLY_EXIT_DATA`를 완전히 교체

---

#### [MODIFY] [build_80percent_early_exit_pdf.py](file:///g:/내 드라이브/Board Game Ideas/291. Co-op Climbing/build_80percent_early_exit_pdf.py)

- 데이터 구조 변경 시 PDF 빌더도 함께 업데이트 (margin 등)

## 실행 순서

1. `reoptimize_80percent.py` 작성
2. 실행하여 40레벨 × 4인원수 = 160개 조합 최적화 (약 5~10분 소요 예상)
3. 결과 검증 — 각 레벨별 목표 승률 대비 실제 승률 오차 확인
4. `calibrated_80percent_early_exit.py` 업데이트
5. `build_80percent_early_exit_pdf.py` 실행하여 PDF 재생성

## 목표 품질 기준

| 항목 | 기준 |
|------|------|
| 목표 승률 매칭 | 각 인원수별 ±8%p 이내 (이상적으로 ±5%p) |
| LV1-LV2 변별력 | 최소 1개 인원수에서 5%p 이상 차이 |
| 인접 레벨 변별력 | 같은 인원수에서 실제 승률이 단조감소 경향 유지 |

## Open Questions

> [!IMPORTANT]
> **margin(허용 오차) 값도 조정 대상으로 넣을까요?** 현재 모든 레벨에서 margin=2로 고정되어 있습니다. margin을 레벨별로 조정하면(예: 쉬운 레벨은 margin=3, 어려운 레벨은 margin=1) 목표 승률 매칭이 훨씬 쉬워집니다. 다만 규칙이 복잡해지는 단점이 있습니다.

> [!IMPORTANT]
> **2인 고스트 미션은 현재 70%로 하드코딩되어 있습니다.** 이것도 시뮬레이션으로 실제 최적화를 할까요, 아니면 70% 고정을 유지할까요?
